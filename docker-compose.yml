services:
  tiny:
    build: .
    pull_policy: build
    environment:
      - STORAGE_ROOT=/data
      - BODY_SIZE_LIMIT=100M
    volumes:
      - data:/data
    restart: unless-stopped

volumes:
  data:
