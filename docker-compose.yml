services:
  tiny:
    build: .
    pull_policy: build
    environment:
      - STORAGE_ROOT=/data
      - USERS_SECRET=$USERS_SECRET
      - BODY_SIZE_LIMIT=100M
    volumes:
      - data:/data
    restart: unless-stopped

volumes:
  data:
