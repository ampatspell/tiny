{
  "name": "player",
  "scripts": {
    "fetch": "node tiny/fetch",
    "gen": "node tiny/generate",
    "fgen": "npm run fetch && npm run gen"
  },
  "dependencies": {
    "tiny-export": "^0.5.0"
  }
}
