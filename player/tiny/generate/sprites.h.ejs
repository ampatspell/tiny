#pragma once

#include <stdint.h>

// Project: <%= project.title %>
// Sprites: <%= sprites.length %>

class Sprite;

namespace Tiny {
  namespace Sprites {
    Sprite *getSprite(uint8_t index);
  }
}
