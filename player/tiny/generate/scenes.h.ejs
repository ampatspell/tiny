#pragma once

// Project: <%= project.title %>
// World: <%= world.name %>
// Scenes: <%= scenes.length %>

#include <stddef.h>
#include <stdint.h>

class Scene;

<% scenes.forEach(scene => { %>
<%= include('scene.h', { project, world, scene }) %>
<% }) %>

namespace Tiny {
  namespace Scenes {

    size_t getStorageSize();

    Scene *instantiate(uint8_t index);

  }
}
