#pragma once

#include <stdint.h>

namespace Tiny {

class Scene;

namespace Project {
namespace Scenes {
<% project.scenes.forEach(scene => { -%>
<%= include('scene.h', { project, scene }) -%>
<% }) -%>

uint8_t numberOfScenes();

Scene *instantiateSceneAtIndex(uint8_t index);

}
}
}
