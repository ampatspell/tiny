{{#let "ui-block/sidebar/section/" as |prefix|}}
  {{yield (hash
    disabled=disabled
    header=(component (concat prefix "header"))
    placeholder=(component (concat prefix "placeholder"))
    custom=(component (concat prefix "custom"))
    padded=(component (concat prefix "padded"))
    input=(component (concat prefix "input") disabled=disabled)
    position=(component (concat prefix "position") disabled=disabled)
    size=(component (concat prefix "size") disabled=disabled)
    actions=(component (concat prefix "actions") disabled=disabled)
    segments=(component (concat prefix "segments") disabled=disabled)
    checkmark=(component (concat prefix "checkmark") disabled=disabled)
    alignment=(component (concat prefix "alignment") disabled=disabled)
    flip=(component (concat prefix "flip") disabled=disabled)
    dropdown=(component (concat prefix "dropdown") disabled=disabled)
    properties=(component (concat prefix "properties") disabled=disabled)
  )}}
{{/let}}