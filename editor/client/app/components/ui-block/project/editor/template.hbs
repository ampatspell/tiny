<div class="content">
  {{#ui-konva/autosized class="autosized" as |autosized|}}

    {{#ui-konva/stage
      class="stage"
      size=autosized
      created=(action "created")
      destroying=(action "destroying")
      onClick=(action "deselect")
      draggable=draggable
    }}

      {{#ui-konva/layer props=(hash zIndex=0)}}
        {{ui-konva/project/scenes project=project disabled=draggable}}
      {{/ui-konva/layer}}

      {{#ui-konva/layer props=(hash zIndex=1)}}
        {{ui-konva/project/sprites project=project disabled=draggable}}
      {{/ui-konva/layer}}

    {{/ui-konva/stage}}
  {{/ui-konva/autosized}}
</div>