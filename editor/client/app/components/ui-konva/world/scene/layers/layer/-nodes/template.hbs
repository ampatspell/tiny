{{#each layer.nodes.ordered as |layerNode|}}
  {{component (concat "ui-konva/world/scene/nodes/node/" layerNode.type)
    __index=layerNode.index
    disabled=(or disabled layerNode.locked)
    hidden=(or hidden layerNode.hidden)
    scene=scene
    layer=layer
    layerNode=layerNode
    pixel=pixel
    select=select
    update=update
  }}
{{/each}}