{{component (concat "ui-konva/world/scene/layers/layer/" layer.type)
  disabled=(or disabled layer.locked)
  hidden=(or hidden layer.hidden)
  scene=scene
  layer=layer
  pixel=pixel
  select=select
  update=update
  selected=selected
}}