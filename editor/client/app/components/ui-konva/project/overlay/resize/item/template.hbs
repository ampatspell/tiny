{{ui-konva/rect props=(hash
    x=rect.x
    y=rect.y
    width=rect.width
    height=rect.height
    stroke=color
    strokeWidth=1
    listening=false
)}}

{{ui-konva/text props=(hash
    x=(add rect.x -1)
    y=(add rect.y -11)
    fontFamily="Roboto Mono"
    fontSize=11
    fill="rgba(0,0,0,0.9)"
    text=targetSizeString
    listening=false
)}}

{{ui-konva/project/overlay/resize/item/handle
    key="top"
    handle=handle
    x=position.x.mid
    y=position.y.min
    color=color
    clamp=(action "clamp")
    commit=(action "commit")
}}

{{ui-konva/project/overlay/resize/item/handle
    key="bottom"
    handle=handle
    x=position.x.mid
    y=position.y.max
    color=color
    clamp=(action "clamp")
    commit=(action "commit")
}}

{{ui-konva/project/overlay/resize/item/handle
    key="left"
    handle=handle
    x=position.x.min
    y=position.y.mid
    color=color
    clamp=(action "clamp")
    commit=(action "commit")
}}

{{ui-konva/project/overlay/resize/item/handle
    key="right"
    handle=handle
    x=position.x.max
    y=position.y.mid
    color=color
    clamp=(action "clamp")
    commit=(action "commit")
}}
