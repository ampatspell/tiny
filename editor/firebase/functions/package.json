{
  "name": "tiny-functions",
  "version": "0.7.0",
  "description": "Cloud Functions for Tiny",
  "scripts": {
    "lint": "eslint .",
    "test": "mocha --recursive --reporter spec --slow 10000 --no-timeouts --exit",
    "nyc": "nyc --reporter=html npm run test",
    "bump": "npm version minor --no-git-tag-version",
    "patch": "npm version patch --no-git-tag-version"
  },
  "main": "src/index.js",
  "engines": {
    "node": "8"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "firebase-admin": "~8.5.0",
    "firebase-functions": "^3.2.0",
    "uuid": "^3.3.3"
  },
  "devDependencies": {
    "eslint": "^6.3.0",
    "eslint-plugin-node": "^10.0.0",
    "eslint-plugin-promise": "^4.2.1",
    "firebase-functions-test": "^0.1.6",
    "mocha": "^6.2.0",
    "nyc": "^14.1.1"
  },
  "private": true
}
