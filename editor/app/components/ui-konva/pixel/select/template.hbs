{{#unless disabled}}

  {{ui-konva/pixel/select/overlay
      pixel=pixel
      size=size
      state=state
      begin=(action "beginDraw")
      end=(action "endDraw")
      update=(action "updateDraw")}}

  {{#if state}}
    {{ui-konva/pixel/select/selection
        pixel=pixel
        size=size
        state=state
        begin=(action "beginMove")
        move=(action "updateMove")
        end=(action "endMove")
    }}
  {{/if}}

{{/unless}}