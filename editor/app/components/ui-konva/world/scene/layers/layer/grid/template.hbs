{{ui-konva/world/scene/layers/layer/grid/background
  grid=layer.grid
  size=scene.size
  background=scene.background
  pixel=pixel
}}

{{ui-konva/world/scene/layers/layer/-nodes
  disabled=disabled
  hidden=hidden
  scene=scene
  layer=layer
  pixel=pixel
  select=select
  update=update
}}
